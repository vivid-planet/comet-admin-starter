<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Comet Admin Starter</title>
        <script type="text/javascript">
            (function() {
<%= environmentValues.map((env) => `window.EXTERNAL__${env.key}__ = "${env.value}";`).join("\n") %>
            })();
        </script>
    </head>
    <body>
        <div id="page">
            <comet-admin-starter />
        </div>
    </body>
</html>
