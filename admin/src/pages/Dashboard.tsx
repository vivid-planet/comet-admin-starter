import { Typography } from "@material-ui/core";
import { Stack } from "@vivid-planet/comet-admin";
import * as React from "react";

export default () => {
    return (
        <Stack topLevelTitle="Dashboard">
            <Typography>Dashboard</Typography>
        </Stack>
    );
};
