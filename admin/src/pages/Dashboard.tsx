import { Typography } from "@material-ui/core";
import { Stack } from "@vivid-planet/react-admin-core";
import * as React from "react";

export default () => {
    return (
        <Stack topLevelTitle="Dashboard">
            <Typography>Dashboard</Typography>
        </Stack>
    );
};
